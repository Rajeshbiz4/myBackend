#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../shipit-cli/bin/shipit" "$@"
  ret=$?
else 
  node  "$basedir/../shipit-cli/bin/shipit" "$@"
  ret=$?
fi
exit $ret
